name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Hoặc 'master' tùy vào nhánh chính của bạn

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Thư mục chứa build output của Vite
          branch: gh-pages # Nhánh mà GitHub Pages sẽ sử dụng
